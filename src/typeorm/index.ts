import { User } from './user.entity';
import { Address } from './address.entity';
import { Post } from './post.entity';
const entities = [User, Address, Post];

export { User, Address, Post };
export default entities;
